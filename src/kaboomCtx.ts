import kaboom from "kaboom";

const k = kaboom({
  width: 960,
  height: 540,
  scale: 1,
  canvas: document.getElementById("canvas") as HTMLCanvasElement,
});

export const {
  add,
  rect,
  pos,
  outline,
  area,
  debug,
  text,
  anchor,
  color,
  width,
  center,
  height,
  opacity,
  scene,
  go,
  vec2,
  setCursor,
  scale,
  rgb,
  loadSprite,
  loadSpriteAtlas,
  sprite,
  body,
  addLevel,
  setGravity,
  Rect,
  onKeyDown,
  onKeyRelease,
  onKeyPress,
  destroyAll,
  isKeyDown,
  tween,
  easings,
  rotate,
  state,
  wait,
  onUpdate,
} = k;
